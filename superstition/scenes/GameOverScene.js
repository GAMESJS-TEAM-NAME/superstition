function GameOverScene(){
    this.setup = ()=>{
        background(255);
        push();
        fill(0);
        textSize(34);
        text("Game Over!" , width/2 , height/2);
        pop();
    }

    this.draw = ()=>{
        
    }

}